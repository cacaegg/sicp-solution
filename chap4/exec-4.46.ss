;; If the amb evaluator evalute the argument from right to left order,
;; then the grammer can't match to input. 
;; Since the input will always comes in with left to right order,
;; change the evaluation order will make a incorrect grammer result.
